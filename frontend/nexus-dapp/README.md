# Nexus Protocol

Premium DeFi staking protocol interface built with React, TypeScript, Tailwind CSS, Framer Motion, and mock protocol data.

## Scripts

```bash
npm install
npm run dev
npm run build
```

The app entry point is `src/App.tsx`, with reusable UI components in `src/components` and mock data in `src/data/mockData.ts`.
